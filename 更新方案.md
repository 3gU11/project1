  1.1 备份现有系统
                                                                                                                                            # 2. 备份数据库
  mysqldump -u root -p your_password rjfinshed > D:\backup\rjfinshed_old_$(date +%Y%m%d).sql

  # 2. 备份文件
  xcopy D:\antigraveity_pj\V7ex D:\backup\V7ex_old_$(date +%Y%m%d) /E /H /C /I

  # 3. 备份配置
  copy D:\antigraveity_pj\V7ex\.env D:\backup\.env_$(date +%Y%m%d)

  1.2 准备部署文件

  # 在本地打包新版本
  cd D:\trae_projects\V7change\V7ex
  zip -r V7ex_new.zip .

  阶段2：数据库迁移（2-3小时）

  2.1 创建新数据库结构

  -- 在服务机MySQL中执行
  CREATE DATABASE rjfinshed_new;
  USE rjfinshed_new;

  -- 执行新版本的database.py中的建表语句
  -- 或者使用新版本的初始化脚本

  2.2 数据迁移脚本

  # 在服务机创建数据迁移脚本
  import pandas as pd
  from sqlalchemy import create_engine

  # 连接旧数据库
  old_engine = create_engine('mysql+pymysql://root:030705@localhost/rjfinshed')

  # 连接新数据库
  new_engine = create_engine('mysql+pymysql://root:030705@localhost/rjfinshed_new')

  # 迁移关键表
  tables_to_migrate = ['users', 'contracts', 'shipping_history', 'machine_archives']
  for table in tables_to_migrate:
      df = pd.read_sql_table(table, old_engine)
      df.to_sql(table, new_engine, if_exists='replace', index=False)

  2.3 初始化新系统

  # 运行新版本的初始化脚本
  python database.py init_mysql_tables

  阶段3：代码部署（1-2小时）

  3.1 传输文件到服务机

  # 使用scp或文件共享传输
  scp V7ex_new.zip user@server_ip:/path/to/deploy/

  # 或通过文件共享复制
  copy D:\trae_projects\V7change\V7ex\* \\server_ip\share\path\

  3.2 解压和配置

  # 在服务机解压
  powershell Expand-Archive -Path "D:\deploy\V7ex_new.zip" -DestinationPath "D:\antigraveity_pj\V7ex_new"

  # 复制配置文件
  copy D:\antigraveity_pj\V7ex\.env D:\antigraveity_pj\V7ex_new\.env

  # 安装依赖
  cd D:\antigraveity_pj\V7ex_new
  pip install -r requirements.txt

  3.3 调整配置

  # 修改config.py中的数据库配置
  # 确保指向新数据库
  MYSQL_DB = "rjfinshed_new"

  阶段4：服务启动和测试（2-3小时）

  4.1 启动新服务

  # 停止旧服务
  taskkill /F /IM python.exe

  # 启动新服务
  cd D:\antigraveity_pj\V7ex_new
  python run_api.py

  4.2 功能测试

  # 测试API接口
  curl -X GET "http://localhost:8000/api/v1/health" -H "Authorization: Bearer your_token"

  # 测试前端访问
  start http://localhost:8501  # 如果使用Streamlit
  start http://localhost:8080  # 如果使用Vue

  阶段5：验证和切换（1小时）

  5.1 数据验证

  # 验证数据完整性
  def verify_data_migration():
      # 检查关键表数据
      old_count = get_old_data_count()
      new_count = get_new_data_count()
      assert old_count == new_count, "数据迁移不一致"

  5.2 切换域名/端口

  # 修改nginx或IIS配置指向新服务
  # 重启web服务器

  回滚方案

  6.1 快速回滚

  # 停止新服务
  taskkill /F /IM python.exe

  # 启动旧服务
  cd D:\antigraveity_pj\V7ex_old
  python run_api.py

  # 恢复数据库
  mysql -u root -p rjfinshed < D:\backup\rjfinshed_old_$(date +%Y%m%d).sql

  部署时间安排

  ┌────────────┬─────────┬────────┬────────────────┐
  │    阶段    │  时间   │ 负责人 │      备注      │
  ├────────────┼─────────┼────────┼────────────────┤
  │ 准备备份   │ 1-2小时 │ 运维   │ 低峰期操作     │
  ├────────────┼─────────┼────────┼────────────────┤
  │ 数据库迁移 │ 2-3小时 │ DBA    │ 核心步骤       │
  ├────────────┼─────────┼────────┼────────────────┤
  │ 代码部署   │ 1-2小时 │ 开发   │ 文件传输和配置 │
  ├────────────┼─────────┼────────┼────────────────┤
  │ 服务启动   │ 2-3小时 │ 运维   │ 测试验证       │
  ├────────────┼─────────┼────────┼────────────────┤
  │ 切换上线   │ 1小时   │ 运维   │ 正式切换       │
  └────────────┴─────────┴────────┴────────────────┘

  注意事项

  1. 数据库备份：务必在低峰期进行完整备份
  2. 依赖检查：确认服务机Python版本兼容
  3. 网络配置：确保服务机端口开放
  4. 监控设置：部署后监控系统状态
  5. 用户通知：提前通知用户维护时间