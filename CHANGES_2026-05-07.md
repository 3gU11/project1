# V7STD1.0 变更记录

**日期**: 2026-05-07  
**分支**: main

---

## 一、沙盘特殊机型列修复

### 概述
修复沙盘“信息强改”抽屉中，特殊机型列可选择普通机型或大类占位值的问题。

### 更新内容
- 特殊列机型下拉框只显示机型字典中归属为“特殊”的具体机型。
- 过滤 `SPECIAL`、`AUTO`、`XS`、`G` 等大类占位值，避免它们被写入卡片机型。
- 特殊列编辑时禁用自由创建机型，普通列保持原有可创建行为。
- 保存信息强改时校验机型与当前批次系列匹配，不允许写入跨系列机型。

---

## 二、沙盘拖拽规则修复

### 概述
修复特殊列、大机列、小机列之间拖拽后可能出现跨列数据不一致的问题。

### 更新内容
- 普通批次内拖拽限制为同系列、同大小机列。
- 保留“大机插入或转入特殊列后，可拖回对应大机列”的例外能力。
- 增加混放数量提示，批次标题显示“混放 N”。
- 对混放卡片增加视觉标记，便于定位异常数据。
- 拖拽成功后临时保持当前批次顺序，避免自动排序立即打乱用户操作结果。
- 手动点击“刷新”后恢复自动排序。

---

## 三、卡片与急单字段一致性

### 概述
补齐沙盘卡片展示和急单插入链路中的订单字段，减少合同信息在顺延过程中的丢失。

### 更新内容
- 卡片增加合同备注展示。
- 已确认或生产中卡片显示流水号。
- 急单插入时携带 `remark/order_remark` 字段。
- 急单顺延写入时同步保留 `sales_id`、`order_remark` 等订单字段。
- 急单自动插入、手动插入、拖拽插入都保持一致字段传递。

---

## 四、老板计划与状态同步

### 概述
补齐老板计划 `factory_plan` 与沙盘/生产看板之间的状态同步，避免订单已进入生产但老板计划仍停留在待规划状态。

### 更新内容
- 系统初始化时，将 `factory_plan` 中空状态或 `未下单` 规范为 `待规划`。
- 批次分配到产线时，按 `合同号 + 机型` 将老板计划状态从 `待规划` 更新为 `已规划`。
- 批次分配接口响应增加：

```json
{
  "factory_plan_status_update": {
    "pairs": 0,
    "rows": 0
  }
}
```

- `pairs` 表示本批次中参与同步的合同号/机型组合数。
- `rows` 表示实际更新的老板计划行数。
- 急单插入沙盘后，同步将对应老板计划记录标记为 `已规划`。

---

## 五、成品库同步

### 概述
急单插入和内容交换后，同步更新 `finished_goods_data`，保证成品库占用信息与沙盘卡片一致。

### 更新内容
- 根据机台流水号候选值匹配成品库记录。
- 同步字段包括：合同号、占用订单号、客户、代理商、合同备注、状态。
- 有合同号时，成品库状态更新为 `待发货`。
- 无合同号时，成品库状态回到 `待入库`。
- 同步逻辑兼容不同成品库表结构：字段不存在时跳过对应字段。

---

## 六、批次顺延规则修复

### 概述
修复沙盘整理批次时，小机/大机容量不同的批次可能互相顺延顶出的风险。

### 更新内容
- `enforceFamilyGapDays` 顺延时，不再简单使用下一个批次。
- 改为寻找后续相同容量批次进行顺延。
- 若后续无相同容量批次，则进入溢出队列。
- 该规则降低了跨大小机列导致的数据错位风险。

---

## 七、已回退内容

### 标记现货同步老板计划
- 已回退“标记现货时同步取消 `factory_plan`”的实现。
- 当前 `mark-spot` 行为恢复为：只清空卡片合同字段并解锁。
- 当前 `mark-spot` 不修改老板计划状态。

### 标记现货合同回流沙盒
- 已回退“标记现货后释放合同回流 Predicted 沙盒链”的实现计划。
- 当前标记现货不会把释放合同重新插入沙盒。
- 后续如需实现，需要重新确认“取消合同”和“回流沙盒”是否应同时影响 `factory_plan`。

---

## 八、验证记录

- 后端 `go test ./...` 已通过。
- 前端构建曾用于验证特殊机型下拉过滤和拖拽规则修复。
- 已确认 `mark-spot` 中不再包含 `factory_plan` 取消同步逻辑。

---

## 九、未纳入本变更记录的工作区内容

当前工作区存在缓存、日志、构建产物等非功能性变动，不纳入本变更记录：

- Python `__pycache__`
- Go `.gocache` / `.gocache-build`
- 前端 `dist` 构建产物
- 启动日志、服务输出日志
- 临时测试可执行文件

---

# V7STD1.0 变更记录（2026-05-09）

**日期**: 2026-05-09
**分支**: main

---

## 一、合同取消级联清理：同步清除急单队列

### 概述
修复"标记现货"和"合同管理强制取消"两条路径中，rush_order_queue（急单队列）未同步清除的问题。

### 更新内容
- Go 后端 MarkSpot（server/internal/handler/unit.go）：事务中新增 UPDATE rush_order_queue SET status='deleted' WHERE contract_no=? AND status='pending'，确保沙盘标记现货时同合同急单卡片一并消失。
- Python 后端 update_contract_status（api/routes/planning.py）：合同管理页"已取消"操作同步更新 rush_order_queue。
- 前端 ProductionKanban.vue：forceRefreshAll() 新增调用 safeFetchRushOrders()，标记现货后急单队列面板自动刷新。

---

## 二、订单配货与老板计划（沙盘）双向同步

### 概述
订单配货的"配货"与"撤回配货"操作，现在会同步更新沙盘 units 表中对应机台的归属信息。

### 更新内容（crud/orders.py）

#### 配货（allocate_inventory）
- 配货成功后，通过 serial_no / forecast_serial_no 匹配，将 customer、dealer_name 同步写入 units 表。
- contract_no、due_date 保持沙盘原有值不覆盖，避免破坏生产计划绑定。
- 核心修复：将原来的"全量 DataFrame 读-改-存"改为直接 SQL UPDATE，彻底解决缓存旧值覆盖机台真实状态（如"库中A01区"）的 bug。

#### 撤回配货（revert_to_inbound）
- 撤回时清除：占用订单号、客户、代理商、合同号。
- 保留合同备注（含加高、特殊配置等非标记录，跟随机台而非订单）。
- 同步清空 units 表对应机台的 contract_no、customer、dealer_name、sales_id、due_date 并解锁（is_locked=0）；order_remark 保留。

---

## 三、移除 get_data() 的 lru_cache

### 概述
get_data() 原使用 @lru_cache(maxsize=1) 永久缓存，导致写入操作后缓存未刷新时读到脏数据；全量 DELETE+INSERT 的 save_data() 与缓存联用时，旧缓存会覆盖数据库中已更新的机台状态。

### 更新内容
- crud/inventory.py：移除 get_data() 的 @lru_cache 装饰器，每次直接查数据库。
- 清理所有 get_data.cache_clear() 调用（crud/inventory.py、crud/orders.py、api/routes/planning.py、utils/cache_adapter.py）。
- get_data_v2() 的 @ttl_cache(ttl_seconds=30) 保留不变。

---

## 四、发货复核历史数据修复

### 问题
SO-20260509-5536 状态已为 ready，但 10 台关联机台状态仍为"库中A01区"/"待入库"，未出现在发货复核页面。

### 根因
配货时旧 DataFrame 缓存覆盖了机台真实入库状态，机台状态未被正确改为"待发货"。

### 处置
- 直接 SQL 修复：将 10 台机台状态更新为"待发货"。
- 根本修复：见第三节"移除 lru_cache"。

---

## 五、验证说明

- Go 后端编译通过（go build ./...）。
- Python 模块导入验证通过（get_data 已确认为普通函数）。
- 所有改动需重启 Python FastAPI 服务 + Go 服务后生效。

---

## 六、新增机台“已绑定”专属状态及库存查询界面优化

### 概述
为有效区分“名花有主”的在制机台与“公共现货”的在制机台，在成品库存中引入了全新的机台状态 `已绑定`。

### 更新内容
- **后端导入逻辑增强**：修改 `api/routes/sandbox.py` (沙盘批次写入) 与 `utils/parsers.py` (正式导入成品库) 中的状态指派逻辑：当检测到沙盘排产队列机台附带 `合同号` 时，成品库和导入缓存的初始状态记录为 `已绑定`；无合同号时则依然为 `待入库`。
- **修复备注字段丢失**：修正 `utils/parsers.py` 在写入 `finished_goods_data` 时由于使用了未在 `INVENTORY_COLS` 内注册的字段名（如“机台备注/配置”、“订单备注”），导致合同备注在落库时被框架丢弃的 Bug。现已规范统一为写入 `合同备注` 字段。
- **入库审核列表优化**：得益于状态文本隔离，`Inbound.vue` 的“待入库清单”由于过滤规则 `includes('待入库')`，会自然屏蔽 `已绑定` 状态的机台。
- **全局库存查询面板 (`InventoryQuery.vue`) 增强**：
  - 核心过滤放行：调整底层 `inventoryFilter.ts` 数据屏障规则，允许 `已绑定` 状态的在途机台流向前端渲染层。
  - 概览指标扩充：总览大盘右侧新增并排的 `🔗 已绑定 (Bound)` 数据块与计数指标。
  - 筛选项扩展：顶部筛选下拉框增加“已绑定”独立选项卡。
  - 数据模型贯通：同步修订 `inventoryStats.ts` 数据统计内核，让系统统计表格可以分列出每款机型的“库存中”、“待入库”、“已绑定”及“全部”情况。

---

## 七、合同录入即时同步沙盘（自动触发全量重算）

### 背景与问题
原有流程中，合同录入后须手动点击"全量重算"才能在预测沙盘中看到对应卡片，体验割裂；且 `production_queue` 仅作为算法溢出缓冲，普通合同绕过队列直通 `units` 的机制造成理解混乱。

### 解决方案
将普通合同录入与沙盘刷新解耦，录入成功后由后端在后台线程自动向 Go 服务发起全量重算请求，消除手动点击步骤。

### 更新内容

#### `api/routes/planning.py`
- **新增依赖导入**：引入 `BackgroundTasks`（FastAPI）、`httpx`、`GO_SANDBOX_URL`、`GO_INTERNAL_TOKEN`。
- **新增函数 `_trigger_sandbox_recompute_sync(user_ctx)`**：使用 `httpx.Client` 同步调用 Go 服务 `POST /api/forecast/recompute`，携带用户身份头及内部鉴权 Token；异常时仅记录 warning，不影响主流程。
- **修改 `create_contracts_batch`**：
  - 函数签名加入 `background_tasks: BackgroundTasks`。
  - 非急单录入成功后，将 `_insert_production_queue(add_list)` 替换为 `background_tasks.add_task(_trigger_sandbox_recompute_sync, current_user)`，录入响应立即返回，重算在后台异步执行。
  - 急单（`is_rush=True`）路径不变，仍走 `rush_order_queue`。

#### `frontend/vite.config.ts`
- 在 `proxy['/api']` 配置中补充 `ws: true`，修复开发环境下 WebSocket 升级请求未被正确代理至后端的问题（表现为全量重算时控制台抛出 `WebSocket connection failed`）。

### 新的数据流

`
合同录入 (POST /contracts/batch-create)
  ↓ 写入 factory_plan（主流程，同步）
  ↓ background_tasks → _trigger_sandbox_recompute_sync
      ↓ Go /api/forecast/recompute（后台异步，约 1~2s）
          ↓ FullRecompute 算法
              ↓ units 表更新
                  ↓ 沙盘卡片即时可见（刷新页面或 WebSocket 推送）
`

### 注意事项
- 重启 Python FastAPI 服务后生效。
- 前端 Vite 代理的 `ws: true` 改动需重启 `npm run dev` 后生效（构建配置不支持热更新）。
- `_insert_production_queue` 函数保留供急单溢出和历史兼容使用，未删除。
