# V7STD1.0 变更记录

**日期**: 2026-05-07  
**分支**: main

---

## 一、plan_import 表列结构调整

### 概述
将 `plan_import` 表中 `机台备注/配置` 列替换为 `客户`、`代理商`、`合同备注` 三列，以容纳沙盘 cards 中 units 表的对应内容。

### 修改文件

#### 1. `database.py` — Schema 升级 v1 → v2
- `CURRENT_SCHEMA_VERSION` 从 1 升至 2
- `CREATE TABLE plan_import` DDL: `机台备注/配置 TEXT` → `客户 VARCHAR(200) + 代理商 VARCHAR(200) + 合同备注 TEXT`
- `init_mysql_tables()`: 新增 v1 原地迁移（ADD COLUMN + DROP COLUMN）
- `init_mysql_tables_v2()`: 新增 v1→v2 版本化迁移块，调用 `_record_schema_version(conn, 2, ...)`
- `model_dictionary` 表新增 `model_family VARCHAR(32)` 列

#### 2. `crud/inventory.py`
- `IMPORT_COLS`: `["流水号", "批次号", "机型", "状态", "预计入库时间", "机台备注/配置"]` → `["流水号", "批次号", "机型", "状态", "预计入库时间", "客户", "代理商", "合同备注"]`
- `delete_import_staging_by_serials()`: 返回值从 `int` 改为 `{"deleted": int, "orphaned_batch_codes": list}`，删除前收集受影响的批次号，删除后检测已被清空的批次号

#### 3. `api/routes/inventory.py`
- 删除 `from utils.parsers import ...` 导入（build_import_payload 等）
- 删除 `ImportConfirmPayload` 和 `AutoGeneratePayload` Pydantic 模型
- 新增 `_auto_revoke_sandbox_batches()` 函数：当 plan_import 中某批次号的所有记录被删除后，自动将沙盘 `batches` 表中对应批次状态从 `Confirmed` 回退为 `Predicted`
- `delete_import_staging_rows` 端点：调用 `_auto_revoke_sandbox_batches()` 处理孤儿批次号

#### 4. `api/routes/sandbox.py`
- `sync_to_plan`: units 数据写入 plan_import 时，分别写入 `客户`、`代理商`、`合同备注` 字段（不再拼接为 `机台备注/配置`）

#### 5. `utils/parsers.py`
- `generate_auto_inbound()`: `机台备注/配置` → `合同备注`
- `parse_tracking_xls()`: col3 写入 `合同备注` 而非 `机台备注/配置`
- `execute_import_transaction_payload()`: 从 plan_import 读取 `客户`、`代理商`、`合同备注`，映射 `合同备注` → finished_goods_data 的 `机台备注/配置`

#### 6. `frontend/src/views/Inbound.vue`
- Tab1 待入库设备清单表格：`机台备注/配置` 列替换为 `客户`、`代理商`、`合同备注` 三列
- Tab2 待入库数据审核表格：同上替换，三列均可 inline 编辑
- `fetchImportStaging()`: 数据映射字段对应更新

#### 7. `tests/test_auto_inbound_integration.py`
- 测试表 schema 更新：`机台备注/配置` → `客户`、`代理商`、`合同备注`

---

## 二、产线分配时自动导入 finished_goods_data

### 概述
生产看板中将 Confirmed 批次分配到产线后，自动将 plan_import 中该批次的记录导入到 finished_goods_data（成品库存），无需再到入库页面手动操作。

### 数据流
```
用户点击"整批分配" → 选择产线 → 确认
  ↓
(1) Go AssignToLine → batch/line/units → In_Production
  ↓
(2) POST /sandbox/batches/:id/import-to-finished-goods
      → 查 batches.batch_code → 查 plan_import WHERE 批次号 = batch_code
      → 调用 execute_import_transaction_payload()
      → plan_import 记录移至 finished_goods_data
  ↓
(3) forceRefreshAll()
```

### 修改文件

#### 1. `api/routes/sandbox.py` — 新增端点
- `POST /sandbox/batches/{batch_id}/import-to-finished-goods`
- 逻辑：查 batch_code → 查 plan_import → 构建 payload → 调用 `execute_import_transaction_payload()` → 返回 `{success, count, failed_count}`

#### 2. `frontend/src/services/sandboxApi.ts` — 新增 API 函数
```typescript
export const importBatchToFinishedGoods = (batchId: string) =>
  apiPost(`${P}/batches/${batchId}/import-to-finished-goods`)
```

#### 3. `frontend/src/views/sandbox/ProductionKanban.vue` — 修改 doAssign()
- 在 `lineStore.assignLine()` 成功后、`ElMessage.success` 之前插入自动导入调用
- 导入失败不影响分配主流程（catch 静默处理）

---

## 三、批量导入跟踪单功能清理

### 概述
移除"上传跟踪单"、"确认导入"、"自动生成流水号"三个功能模块，仅保留"待入库数据审核"表格。

### 修改文件

#### 1. `api/routes/inventory.py` — 删除端点
- 删除 `POST /import-staging/upload`（上传跟踪单解析）
- 删除 `POST /import-staging/import-confirm`（确认导入）
- 删除 `POST /import-staging/auto-generate`（自动生成流水号）

#### 2. `utils/parsers.py` — 删除死代码
- 删除 `generate_auto_inbound()` 函数
- 删除 `parse_tracking_xls()` 函数
- 删除 `diff_tracking_vs_inventory()` 函数
- 删除 `build_import_payload()` 函数
- 清理废弃 import: `os`, `subprocess`, `tempfile`, `MACHINE_ARCHIVE_ABS_DIR`, `OPENPYXL_AVAILABLE`, `openpyxl`, `append_import_staging_transactional`
- 保留: `execute_import_transaction_payload()`（沙盘自动导入仍在使用）

#### 3. `frontend/src/views/Inbound.vue` — UI 清理
- 删除"上传跟踪单据"卡片（含文件选择器和解析按钮）
- 删除"确认导入"按钮
- 删除"自动生成流水号"卡片（含批次号/机型/数量/日期/备注输入和生成按钮）
- 删除关联变量: `trackingFileRef`, `uploading`, `confirmingImport`, `autoGenerating`, `autoGen`, `canConfirmImport`, `submitWithLock`
- 删除关联函数: `uploadTracking()`, `confirmImport()`, `submitAutoGenerate()`
- 删除关联 import: `reactive`, `useFormSubmit`, `ImportConfirmResponse`

#### 4. `tests/test_auto_inbound_integration.py`
- 删除 `test_generate_auto_inbound_returns_readable_error_code` 测试
- 删除 `from utils import parsers` 导入

#### 5. `scripts/connectivity_e2e_runner.py`
- 删除 `_step_auto_generate_invalid_qty`, `_step_auto_generate_one`, `_step_pick_serial`, `_step_import_confirm_empty`, `_step_import_confirm` 方法
- run() 流程中移除对应步骤调用
- blockers 集合中移除 `auto_generate_one` 和 `import_confirm_valid`

---

## 四、启动时自动数据库迁移

### 修改文件

#### `api/main.py`
- 新增 `@app.on_event("startup")` 钩子
- 启动时自动调用 `init_mysql_tables_v2()` 执行 schema 迁移
- 注册 sandbox 路由和 WebSocket 端点

---

## 影响范围汇总

| 层级 | 文件 | 变更类型 |
|------|------|----------|
| 数据库 | `database.py` | Schema v1→v2, 列替换, 版本迁移 |
| 后端 CRUD | `crud/inventory.py` | 列名更新, 删除返回值增强 |
| 后端 API | `api/routes/inventory.py` | 删除 3 个端点, 新增自动撤销 |
| 后端 API | `api/routes/sandbox.py` | 列写入更新, 新增自动导入端点 |
| 后端 API | `api/main.py` | 启动迁移, sandbox 路由注册 |
| 后端工具 | `utils/parsers.py` | 删除 4 个死函数, 列映射更新 |
| 前端 | `Inbound.vue` | 列替换, 删除上传/确认/自动生成 UI |
| 前端 | `sandboxApi.ts` | 新增 importBatchToFinishedGoods |
| 前端 | `ProductionKanban.vue` | doAssign() 增加自动导入调用 |
| 测试 | `test_auto_inbound_integration.py` | Schema 更新, 删除废弃测试 |
| 脚本 | `connectivity_e2e_runner.py` | 删除废弃 E2E 步骤 |

---

## 关键保留项

- `execute_import_transaction_payload()` — 仍被沙盘自动导入端点调用
- 待入库数据审核表格 — Tab2 核心功能保留, 支持 inline 编辑/保存/删除
- 设备扫描入库 (Tab1) — 完整保留
- 沙盘预测/审核/分配/生产看板 — 完整保留
