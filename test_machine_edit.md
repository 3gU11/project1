# 机台编辑功能测试验证

## 测试目标
验证移除"仅允许同生产组改型"限制后，允许同批次混合 FT/G/XS 等不同族类机型

## 测试环境
- 前端: http://localhost:8888
- 后端API: http://localhost:8000
- 测试页面: 机台信息编辑 (MachineEdit.vue)

## 测试用例

### 用例1：跨族类改型（核心功能）
**目的**: 验证可以将机台从一个族类改为另一个族类

**前置条件**:
- 选择至少1台未绑定的机台
- 机台当前机型属于某个族类（如中小型XS）

**测试步骤**:
1. 登录系统，进入"机台信息编辑"页面
2. 在搜索框输入流水号或批次号，筛选机台
3. 勾选1-2台**未绑定合同/订单**的机台（"占用订单号"和"合同号"为空）
4. 在"新的机型"下拉框中选择**不同族类**的机型
   - 例如：原机型是"小机XS"族类，选择"FT特殊"族类的机型
   - 或：原机型是"中大型AUTO"，选择"中小型G"的机型
5. 点击"💾 批量保存修改"按钮

**预期结果**:
- ✅ 显示确认对话框："确认按流水号将 X 台机台机型改为 [目标机型]？"
- ✅ 点击确认后，成功修改，提示"已按流水号修正机型，批次号不变"
- ✅ 刷新数据后，机台的"机型"列显示为新机型
- ❌ **不应该**出现"仅允许同生产组改型"的错误提示

### 用例2：已绑定机台保护（边界测试）
**目的**: 验证已绑定合同/订单的机台仍然被保护

**测试步骤**:
1. 勾选有"占用订单号"或"合同号"的机台
2. 选择任意新机型
3. 点击"💾 批量保存修改"

**预期结果**:
- ❌ 显示错误："已绑定合同或占用订单的机台请到合同管理修改机型：XXX"
- ✅ 机型未被修改

### 用例3：无效机型拒绝
**目的**: 验证只允许已启用的具体机型

**测试步骤**:
1. 勾选未绑定的机台
2. 尝试输入族类占位符（如"XS"、"AUTO"、"G"）或未启用的机型
3. 点击保存

**预期结果**:
- ❌ 显示错误："目标机型未配置族类，无法改型"或"机型不在字典中或未启用"
- ✅ 机型未被修改

### 用例4：批量混合改型
**目的**: 验证批量操作的正确性

**测试步骤**:
1. 勾选3-5台不同机型的未绑定机台（可能来自不同族类）
2. 选择一个新的目标机型
3. 点击保存

**预期结果**:
- ✅ 全部机台成功改为目标机型
- ✅ 批次号保持不变
- ✅ 流水号保持不变

## 手动测试步骤

### 步骤1: 访问测试页面
```bash
# 浏览器打开
http://localhost:8888

# 登录后，点击菜单进入"机台信息编辑"页面
```

### 步骤2: 查找测试数据
在搜索框中输入批次号或流水号，找到几台测试用机台：
- 至少1台未绑定的机台（用于测试跨族类改型）
- 至少1台已绑定的机台（用于测试保护机制）

### 步骤3: 执行测试用例
按上述用例逐一测试

### 步骤4: 检查后端日志
```bash
# 查看API日志，确认没有错误
tail -f api-launcher.out.log | grep -i "machine\|batch"
```

## 数据库验证（可选）

```sql
-- 查看机台修改前后的数据
SELECT 
    `流水号`, 
    `批次号`, 
    `机型`, 
    `占用订单号`, 
    `合同号`, 
    `更新时间`
FROM finished_goods_data
WHERE `流水号` IN ('测试流水号1', '测试流水号2')
ORDER BY `更新时间` DESC;
```

## 审计日志验证（可选）

```sql
-- 查看操作日志
SELECT *
FROM operation_logs
WHERE module = '机台档案'
  AND action_type = '批量修改'
  AND created_at > NOW() - INTERVAL 1 HOUR
ORDER BY created_at DESC
LIMIT 20;
```

## 测试结果记录

| 用例 | 状态 | 备注 |
|------|------|------|
| 用例1: 跨族类改型 | ⏳ 待测试 | |
| 用例2: 已绑定机台保护 | ⏳ 待测试 | |
| 用例3: 无效机型拒绝 | ⏳ 待测试 | |
| 用例4: 批量混合改型 | ⏳ 待测试 | |

## 注意事项

1. **使用测试数据**: 不要在生产数据上测试，避免产生错误的审计记录
2. **记录测试流水号**: 测试前记录机台原始机型，测试后可以恢复
3. **检查关联影响**: 测试后检查是否影响了排产、合同等其他模块
4. **浏览器缓存**: 如果页面未更新，清除浏览器缓存或强制刷新（Ctrl+F5）
