# 排产台账Excel格式 - 完整修改总结

## 修改日期
2026-06-01

## 所有修改内容

### 1. 列结构优化 ✓

**修改前（9列）：**
```
批次号 | 300 | 400 | 500 | 600 | 7055/8055(合并) | 合计 | 预计入库时间
```

**修改后（8列）：**
```
批次号 | 300 | 400 | 500 | 600 | 7055 | 8055 | 合计
```

**改进：**
- ✓ 7055和8055分开成独立列
- ✓ 去掉"预计入库时间"列
- ✓ 8060系列与600系列合并

### 2. 机型匹配逻辑优化 ✓

**修改前：** 短字符串优先匹配，导致误匹配

**修改后：** 长字符串优先匹配
```python
if "8060" in combined:      # 先检查4位数字
    matched_col = "600"
elif "8055" in combined:
    matched_col = "8055"
elif "7055" in combined:
    matched_col = "7055"
elif "600" in combined:     # 再检查3位数字
    matched_col = "600"
# ... 其他
```

### 3. 列索引错误修复 ✓

**问题：** 合计数据被写入8055列，预计入库时间被写入合计列

**修复：**
```python
# 修复前
ws.cell(row=start_row, column=total_cols - 1).value = total_qty  # 错误：第7列

# 修复后
ws.cell(row=start_row, column=total_cols).value = total_qty      # 正确：第8列
```

**删除：** 完全删除预计入库时间写入代码（第2171-2181行）

### 4. 打印设置优化 ✓

**问题：** 行高太高时内容被压缩

**修复：**
```python
# 修改前
ws.page_setup.scale = None
ws.sheet_properties.pageSetUpPr.fitToPage = True  # 自动缩放

# 修改后
ws.page_setup.scale = 100                          # 100%比例，不缩放
ws.sheet_properties.pageSetUpPr.fitToPage = False  # 禁用自动缩放
```

**效果：** 内容完全显示，允许多页打印

### 5. 自动字号调整 ✓

**新增功能：** 根据内容长度自动调整字号

```python
if content_length > 100:
    cell.font = Font(name="宋体", size=7)   # 超长内容
elif content_length > 60:
    cell.font = Font(name="宋体", size=8)   # 长内容
elif content_length > 40:
    cell.font = Font(name="宋体", size=9)   # 中等内容
# else: 默认 size=10                        # 正常内容
```

**效果：** 长文本自动缩小字号，确保内容清晰可读

### 6. 列宽优化 ✓

```
列号 | 列名   | 列宽 | 说明
-----|--------|------|------------------
A    | 批次号 | 10   | 批次编号
B    | 300    | 14   | 300系列（较窄）
C    | 400    | 20   | 400系列
D    | 500    | 20   | 500系列
E    | 600    | 20   | 600+8060系列
F    | 7055   | 20   | 7055系列（独立）
G    | 8055   | 20   | 8055系列（独立）
H    | 合计   | 8    | 总数量
```

**总宽度：132** (适合A4横向打印)

## 修改的文件

**api/routes/planning.py**
1. 第2039行：列定义 - 添加8055列
2. 第2058行：表头定义 - 去掉预计入库时间
3. 第2099-2114行：匹配逻辑 - 长字符串优先
4. 第2159-2177行：写入逻辑 - 自动字号调整
5. 第2167-2169行：合计列索引修正
6. 第2171-2181行：删除预计入库时间写入代码
7. 第2183-2193行：打印设置优化

**tests/test_excel_export.py**
- 第71行：更新测试用例
- 第157行：更新测试用例

## 最终效果

### 列结构
✓ 8列：批次号、300、400、500、600、7055、8055、合计  
✓ 7055和8055完全分开  
✓ 无预计入库时间列

### 数据显示
✓ 7055列显示FR-7055系列机型  
✓ 8055列显示FR-8055系列机型  
✓ 合计列显示正确的总数量  
✓ 8060系列显示在600列

### 打印效果
✓ 内容不被压缩  
✓ 允许多页打印  
✓ 长文本自动缩小字号  
✓ 总宽度适合A4横向打印

## 字号自动调整规则

| 内容长度 | 字号 | 适用场景 |
|---------|------|---------|
| ≤ 40字符 | 10号 | 正常机型名称 |
| 41-60字符 | 9号 | 带备注的机型 |
| 61-100字符 | 8号 | 长备注信息 |
| > 100字符 | 7号 | 超长备注（如多行说明）|

## API服务状态

✓ API已重启  
✓ 所有修改已生效  
✓ 模拟测试通过

## 验证方法

1. 登录系统
2. 进入排产管理页面
3. 点击"导出排产台账"
4. 下载Excel文件
5. 打开查看：
   - 检查列结构（8列）
   - 检查8055列是否有数据
   - 检查合计列是否正确
6. 打印预览：
   - 检查内容是否完整显示
   - 检查长文本字号是否自动缩小

## 总结

✓ **列结构优化** - 7055和8055分开，去掉预计入库时间  
✓ **匹配逻辑修复** - 长字符串优先，避免误匹配  
✓ **列索引修复** - 合计和8055列数据正确  
✓ **打印优化** - 内容完整显示，不压缩  
✓ **字号自动调整** - 长文本自动缩小，保持清晰  

**所有功能已完成并测试通过！**
