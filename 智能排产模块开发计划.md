# 智能排产模块开发计划

> 目标：在 V7ex 系统中新增独立“智能排产”模块。该模块从合同管理读取合同数据，根据两层产线分配规则和老板设定的完整型号目标库存比例，生成排产草稿；老板确认开始生产后生成流水号并写入 `plan_import`，后续继续走现有成品入库流程。

---

## 1. 业务定位

### 1.1 模块位置

智能排产模块是一个独立模块，不直接替换现有合同管理、成品入库、库存查询和发货流程。

整体数据流：

```text
合同管理 factory_plan
        ↓
智能排产模块 schedule_*
        ↓
老板确认开始生产 / 生成流水号
        ↓
写入 plan_import
        ↓
现有成品入库流程
        ↓
finished_goods_data
        ↓
库存查询 / 配货 / 发货
```

### 1.2 核心业务原则

1. 合同管理仍然是合同数据入口。
2. 智能排产只读取合同管理中的合同数据，不重新做合同录入。
3. 排产草稿阶段只写入智能排产自己的 `schedule_*` 表。
4. 老板确认开始生产时，系统生成流水号。
5. 生成流水号即代表生产启动。
6. 生成流水号后，将机器级清单写入 `plan_import`。
7. `plan_import` 中的机器可以理解为“生产中 / 待入库”的未来库存。
8. 实物入库后进入 `finished_goods_data`。
9. 下一轮排产时，系统要把 `plan_import` 和 `finished_goods_data` 一起作为未来库存基数参与比例平衡。

---

## 2. 核心排产规则

### 2.1 产线分配两层结构

产线分配分为两层。

第一层：机型大类。

```text
小机
大机
稀有机型
```

第二层：版本 / 工艺类型。

```text
XS    普通版本
G     老机型
AUTO  自动穿丝
```

最终产线组示例：

```text
小机 XS 产线组
小机 G 产线组
小机 AUTO 产线组
大机 XS 产线组
大机 G 产线组
大机 AUTO 产线组
稀有机型 XS 产线组
稀有机型 G 产线组
稀有机型 AUTO 产线组
```

第一版不要求每个组合都有实际产线，但数据结构要支持这些组合。

### 2.2 完整型号识别

每个完整型号需要能识别两个维度：

```text
machine_group：SMALL / LARGE / RARE
suffix_type：XS / G / AUTO
```

示例：

| 完整型号 | machine_group | suffix_type |
|---|---|---|
| 400XS | SMALL | XS |
| 400G | SMALL | G |
| 400AUTO | SMALL | AUTO |
| 850MS | RARE | XS 或特殊配置 |
| 大机G | LARGE | G |

第一版可以先通过配置表维护，不建议完全依赖字符串猜测。

若需要字符串兜底解析，顺序必须为：

```text
先判断 AUTO
再判断 G
最后判断 XS
无法识别时默认 XS
```

### 2.3 合同优先覆盖产能

一条产线一列有固定容量，例如 30 台。

合同进入该产线组后，优先覆盖该列前段容量。

示例：

```text
小机 AUTO 线，容量 30
合同：600AUTO × 20

前 20 台：合同 600AUTO
剩余 10 台：预测补产
```

合同不再去“找同型号预测位”，而是直接占据产线列前段。

### 2.4 预测补产用于平衡未来库存比例

老板配置的是“完整型号目标库存比例”，不是单批次固定生产比例。

系统每次排产时要统计未来库存基数：

```text
future_stock = finished_goods_data 中库存中机器
             + plan_import 中生产中 / 待入库机器
             + 本次排产中已经放入的合同段
```

预测段的任务不是机械按 5:3:2 补尾，而是选择最能让未来库存比例接近老板目标比例的型号。

### 2.5 平衡对象

平衡对象不是 `plan_import` 单独一张表，也不是 `finished_goods_data` 单独一张表，而是：

```text
未来有效库存 = finished_goods_data 库存中 + plan_import 生产中/待入库 + 本次排产结果
```

`plan_import` 中的机器将来会进入 `finished_goods_data`，所以排产时必须把它视为未来库存的一部分。

---

## 3. 数据库设计

### 3.1 新增：产线配置表 `schedule_lines`

用途：配置每条产线属于哪个一级机型大类、支持哪些版本/工艺类型、容量是多少。

```sql
CREATE TABLE IF NOT EXISTS schedule_lines (
    id INT NOT NULL AUTO_INCREMENT,
    line_code VARCHAR(50) NOT NULL,
    line_name VARCHAR(100) DEFAULT '',
    machine_group VARCHAR(20) NOT NULL DEFAULT 'SMALL',
    supported_suffixes VARCHAR(100) NOT NULL DEFAULT 'XS',
    default_suffix VARCHAR(20) NOT NULL DEFAULT 'XS',
    capacity INT NOT NULL DEFAULT 30,
    is_flexible TINYINT(1) NOT NULL DEFAULT 0,
    enabled TINYINT(1) NOT NULL DEFAULT 1,
    sort_order INT NOT NULL DEFAULT 0,
    remark VARCHAR(255) DEFAULT '',
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uq_schedule_line_code (line_code),
    INDEX idx_schedule_lines_group (machine_group),
    INDEX idx_schedule_lines_enabled (enabled, sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

示例配置：

| line_code | machine_group | supported_suffixes | capacity | 说明 |
|---|---|---|---:|---|
| L01 | SMALL | XS | 30 | 小机普通线 |
| L02 | SMALL | XS | 30 | 小机普通线 |
| L16 | SMALL | G | 30 | 小机老机型线 |
| L18 | SMALL | AUTO | 30 | 小机自动穿丝线 |
| L20 | SMALL | XS,G,AUTO | 30 | 小机机动线 |
| D01 | LARGE | XS | 15 | 大机线 |
| R01 | RARE | XS,G,AUTO | 5 | 稀有机型线 |

### 3.2 新增：完整型号目标比例表 `schedule_model_ratios`

用途：老板为每个完整型号设置目标库存比例。

注意：这里的比例不是每次排产固定比例，而是目标库存结构比例。

```sql
CREATE TABLE IF NOT EXISTS schedule_model_ratios (
    id INT NOT NULL AUTO_INCREMENT,
    model_name VARCHAR(100) NOT NULL,
    machine_group VARCHAR(20) NOT NULL DEFAULT 'SMALL',
    suffix_type VARCHAR(20) NOT NULL DEFAULT 'XS',
    target_ratio INT NOT NULL DEFAULT 0,
    enabled TINYINT(1) NOT NULL DEFAULT 1,
    sort_order INT NOT NULL DEFAULT 0,
    remark VARCHAR(255) DEFAULT '',
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uq_schedule_model_ratio (model_name),
    INDEX idx_schedule_ratio_group_suffix (machine_group, suffix_type),
    INDEX idx_schedule_ratio_enabled (enabled, sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

示例：

| model_name | machine_group | suffix_type | target_ratio |
|---|---|---|---:|
| 400XS | SMALL | XS | 50 |
| 500XS | SMALL | XS | 30 |
| 600XS | SMALL | XS | 20 |
| 400G | SMALL | G | 5 |
| 500G | SMALL | G | 3 |
| 600G | SMALL | G | 2 |
| 400AUTO | SMALL | AUTO | 8 |
| 500AUTO | SMALL | AUTO | 5 |
| 600AUTO | SMALL | AUTO | 3 |

### 3.3 新增：排产列主表 `schedule_columns`

用途：保存每条产线某个批次/列的排产草稿状态。

```sql
CREATE TABLE IF NOT EXISTS schedule_columns (
    id INT NOT NULL AUTO_INCREMENT,
    batch_no VARCHAR(100) NOT NULL,
    line_code VARCHAR(50) NOT NULL,
    machine_group VARCHAR(20) NOT NULL DEFAULT 'SMALL',
    suffix_type VARCHAR(20) NOT NULL DEFAULT 'XS',
    capacity INT NOT NULL DEFAULT 30,
    planned_start_date DATE NULL,
    planned_finish_date DATE NULL,
    status VARCHAR(50) NOT NULL DEFAULT '草稿',
    imported_to_plan TINYINT(1) NOT NULL DEFAULT 0,
    created_at DATETIME NULL,
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    INDEX idx_schedule_columns_batch (batch_no),
    INDEX idx_schedule_columns_status (status),
    INDEX idx_schedule_columns_line (line_code),
    INDEX idx_schedule_columns_group_suffix (machine_group, suffix_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

状态建议：

```text
草稿
已确认
已投产
已导入待入库
已取消
```

### 3.4 新增：排产列明细表 `schedule_column_items`

用途：保存一列里的合同段和预测段。

```sql
CREATE TABLE IF NOT EXISTS schedule_column_items (
    id INT NOT NULL AUTO_INCREMENT,
    column_id INT NOT NULL,
    segment_order INT NOT NULL DEFAULT 0,
    segment_type VARCHAR(20) NOT NULL DEFAULT 'forecast',
    contract_id VARCHAR(100) DEFAULT '',
    customer VARCHAR(200) DEFAULT '',
    agent VARCHAR(200) DEFAULT '',
    model_name VARCHAR(100) NOT NULL,
    machine_group VARCHAR(20) NOT NULL DEFAULT 'SMALL',
    suffix_type VARCHAR(20) NOT NULL DEFAULT 'XS',
    qty INT NOT NULL DEFAULT 0,
    source_plan_id INT NULL,
    remark TEXT,
    PRIMARY KEY (id),
    INDEX idx_schedule_items_column (column_id),
    INDEX idx_schedule_items_contract (contract_id),
    INDEX idx_schedule_items_model (model_name),
    CONSTRAINT fk_schedule_items_column
        FOREIGN KEY (column_id) REFERENCES schedule_columns(id)
        ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

`segment_type` 可选值：

```text
contract：合同段
forecast：预测段
```

### 3.5 可选：排产库存快照表 `schedule_inventory_snapshot`

第一版可不做，第二版建议加。

用途：记录生成某个批次时，当时每个型号的库存数量、目标比例、当前偏差，便于解释系统为什么补某个型号。

```sql
CREATE TABLE IF NOT EXISTS schedule_inventory_snapshot (
    id INT NOT NULL AUTO_INCREMENT,
    batch_no VARCHAR(100) NOT NULL,
    machine_group VARCHAR(20) NOT NULL,
    suffix_type VARCHAR(20) NOT NULL,
    model_name VARCHAR(100) NOT NULL,
    stock_qty INT NOT NULL DEFAULT 0,
    target_ratio INT NOT NULL DEFAULT 0,
    current_share DECIMAL(10,4) DEFAULT 0,
    target_share DECIMAL(10,4) DEFAULT 0,
    gap_share DECIMAL(10,4) DEFAULT 0,
    created_at DATETIME NULL,
    PRIMARY KEY (id),
    INDEX idx_schedule_snapshot_batch (batch_no, machine_group, suffix_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

## 4. 后端文件结构

新增文件：

```text
crud/scheduling.py
api/routes/scheduling.py
```

修改文件：

```text
database.py
api/main.py
frontend/src/router/index.ts
```

前端新增文件：

```text
frontend/src/api/scheduling.ts
frontend/src/views/SmartScheduling.vue
```

---

## 5. 后端接口设计

### 5.1 路由注册

在 `api/main.py` 中新增：

```python
from api.routes import scheduling

app.include_router(scheduling.router, prefix="/api/v1/scheduling", tags=["Scheduling"])
```

### 5.2 配置接口

产线配置：

```text
GET    /api/v1/scheduling/lines
POST   /api/v1/scheduling/lines
PUT    /api/v1/scheduling/lines/{id}
```

完整型号目标库存比例：

```text
GET    /api/v1/scheduling/ratios
POST   /api/v1/scheduling/ratios
PUT    /api/v1/scheduling/ratios/{id}
```

### 5.3 合同读取接口

```text
GET /api/v1/scheduling/contracts/pending
```

读取 `factory_plan` 中可排产合同。

建议第一版条件：

```sql
SELECT
    id,
    合同号,
    客户名,
    代理商,
    机型,
    排产数量,
    要求交期,
    状态,
    备注
FROM factory_plan
WHERE 状态 NOT IN ('已取消', '已转订单')
ORDER BY 要求交期 ASC, id ASC;
```

后续可排除已经进入 `schedule_column_items.source_plan_id` 的合同。

### 5.4 生成排产草稿接口

```text
POST /api/v1/scheduling/generate
```

请求示例：

```json
{
  "batch_no": "PC-202605-001",
  "planned_start_date": "2026-05-01",
  "planned_finish_date": "2026-05-10",
  "contract_plan_ids": [1, 2, 3],
  "overwrite_existing": false
}
```

返回示例：

```json
{
  "message": "生成成功",
  "columns": [],
  "warnings": [
    "小机 G 类型产能不足，缺口 10 台"
  ]
}
```

### 5.5 看板接口

```text
GET /api/v1/scheduling/columns
GET /api/v1/scheduling/columns/{id}
```

### 5.6 确认 / 生产接口

```text
POST /api/v1/scheduling/columns/{id}/confirm
POST /api/v1/scheduling/columns/{id}/start-production
```

`start-production` 做三件事：

```text
1. 生成流水号
2. 写入 plan_import
3. 更新 schedule_columns.status = 已导入待入库
```

---

## 6. 核心算法设计

### 6.1 型号识别

优先从 `schedule_model_ratios` 读取完整型号配置。

如果完整型号没有配置，可用兜底解析：

```python
def parse_model_type(model_name: str) -> tuple[str, str, str]:
    raw = str(model_name or "").strip()
    upper = raw.upper()

    if upper.endswith("AUTO"):
        return raw[:-4], "SMALL", "AUTO"
    if upper.endswith("G"):
        return raw[:-1], "SMALL", "G"
    if upper.endswith("XS"):
        return raw[:-2], "SMALL", "XS"
    return raw, "SMALL", "XS"
```

### 6.2 统计未来库存基数

统计范围：

```text
plan_import 全部机器
+
finished_goods_data 中状态 LIKE '库存中%'
```

SQL 思路：

```sql
SELECT `机型`, COUNT(*) AS qty
FROM plan_import
GROUP BY `机型`

UNION ALL

SELECT `机型`, COUNT(*) AS qty
FROM finished_goods_data
WHERE `状态` LIKE '库存中%'
GROUP BY `机型`;
```

Python 中合并同型号数量。

### 6.3 计算比例偏差

使用“最小化比例偏差”算法，而不是固定比例补尾。

```python
def calc_deviation(stock: dict[str, int], target_ratios: dict[str, int]) -> float:
    total_stock = sum(stock.get(m, 0) for m in target_ratios)
    total_ratio = sum(target_ratios.values())
    if total_stock <= 0 or total_ratio <= 0:
        return 0.0

    deviation = 0.0
    for model, ratio in target_ratios.items():
        target_share = ratio / total_ratio
        actual_share = stock.get(model, 0) / total_stock
        deviation += abs(actual_share - target_share)
    return deviation
```

选择本次补哪一个型号：

```python
def choose_best_model(stock: dict[str, int], target_ratios: dict[str, int]) -> str:
    best_model = None
    best_score = None

    for model in target_ratios:
        test_stock = dict(stock)
        test_stock[model] = test_stock.get(model, 0) + 1
        score = calc_deviation(test_stock, target_ratios)
        if best_score is None or score < best_score:
            best_score = score
            best_model = model

    return best_model
```

预测分配：

```python
def allocate_forecast_by_future_inventory(stock, target_ratios, qty):
    result = {m: 0 for m in target_ratios}
    simulated = dict(stock)

    for _ in range(qty):
        model = choose_best_model(simulated, target_ratios)
        if not model:
            break
        result[model] += 1
        simulated[model] = simulated.get(model, 0) + 1

    return {m: q for m, q in result.items() if q > 0}
```

### 6.4 生成草稿主流程

```text
1. 读取选择的合同。
2. 根据完整型号配置识别 machine_group 和 suffix_type。
3. 按 machine_group + suffix_type 分组合合同。
4. 读取产线配置。
5. 找到支持该 machine_group + suffix_type 的产线。
6. 专线优先，机动线兜底。
7. 每条产线创建一条 schedule_columns。
8. 合同按数量覆盖该产线前段，写入 contract 段。
9. 将合同数量加入 simulated_stock。
10. 计算该产线剩余容量。
11. 根据该组目标比例和 simulated_stock 生成 forecast 段。
12. forecast 段也加入 simulated_stock。
13. 保存 schedule_column_items。
14. 如果产能不足，返回 warnings。
```

---

## 7. 写入 plan_import 规则

### 7.1 写入时机

只有在老板点击“开始生产 / 生成流水号”时写入 `plan_import`。草稿阶段不得写入 `plan_import`。

### 7.2 流水号规则

第一版建议：

```text
批次号-产线号-三位序号
```

示例：

```text
PC202605001-L01-001
PC202605001-L01-002
PC202605001-L01-003
```

### 7.3 写入内容

将 `schedule_column_items` 的段展开为机器级别行。

字段映射：

| plan_import 字段 | 来源 |
|---|---|
| 流水号 | 系统生成 |
| 批次号 | schedule_columns.batch_no |
| 机型 | schedule_column_items.model_name |
| 状态 | 待入库 |
| 预计入库时间 | schedule_columns.planned_finish_date |
| 机台备注/配置 | 来源：智能排产；类型：合同/预测；合同号；产线；排产列 |

### 7.4 防重复写入

写入前检查：

```text
schedule_columns.imported_to_plan = 0
```

如果已经为 1，禁止重复导入。

---

## 8. 前端页面设计

### 8.1 新增菜单

在 `frontend/src/router/index.ts` 的 `appRouteDefs` 中新增：

```ts
{
  path: '/smart-scheduling',
  name: 'SmartScheduling',
  label: '🧠 智能排产',
  title: '智能排产',
  permission: 'PLANNING',
  isManagement: true,
  component: () => import('../views/SmartScheduling.vue')
}
```

第一版权限可以复用 `PLANNING`。

### 8.2 页面 Tab 设计

建议 `SmartScheduling.vue` 做 4 个 Tab。

1. 排产看板
2. 待排合同
3. 产线配置
4. 目标库存比例

---

## 9. 开发顺序

1. `database.py` 新增 4 张排产表。
2. 新增 `crud/scheduling.py`。
3. 新增 `api/routes/scheduling.py`。
4. `api/main.py` 注册路由。
5. 实现产线配置和目标比例接口。
6. 实现待排合同接口。
7. 实现未来库存统计函数。
8. 实现最小化比例偏差算法。
9. 实现排产草稿生成接口。
10. 新增前端 `scheduling.ts` 和 `SmartScheduling.vue`。
11. 实现开始生产写入 `plan_import`。
12. 联调现有成品入库流程。

---

## 10. 第一版暂不做的功能

```text
1. 自动跨多个批次滚动排产
2. 老板拖拽调整排产段
3. 合同修改后自动回滚/重排
4. 复杂急单审批流
5. 节假日/工作日计算
6. 正式流水号重编号
7. 多用户并发锁定
8. 复杂机动线全局最优分配
```

---

## 11. 验收标准

1. 合同管理导入合同后，智能排产能看到待排合同。
2. `400XS` 进入小机 XS 线。
3. `400G` 进入小机 G 线。
4. `400AUTO` 进入小机 AUTO 线。
5. 大机进入大机池。
6. 稀有机型进入稀有机型池。
7. 合同优先覆盖产线前段。
8. 预测补产根据未来库存比例缺口动态选择型号。
9. 点击开始生产后生成流水号。
10. 写入 `plan_import`。
11. 同一列不能重复导入。

---

## 12. Codex 执行清单

```text
[ ] database.py 新增 schedule_lines 表
[ ] database.py 新增 schedule_model_ratios 表
[ ] database.py 新增 schedule_columns 表
[ ] database.py 新增 schedule_column_items 表
[ ] 新增 crud/scheduling.py
[ ] 新增 api/routes/scheduling.py
[ ] api/main.py 注册 scheduling 路由
[ ] 实现产线配置接口
[ ] 实现目标库存比例接口
[ ] 实现待排合同接口
[ ] 实现 future_stock 统计函数
[ ] 实现最小化比例偏差算法
[ ] 实现排产草稿生成接口
[ ] 实现排产看板接口
[ ] 实现开始生产写入 plan_import 接口
[ ] 新增 frontend/src/api/scheduling.ts
[ ] 新增 frontend/src/views/SmartScheduling.vue
[ ] frontend/src/router/index.ts 新增智能排产菜单
[ ] 联调合同读取
[ ] 联调产线分配
[ ] 联调预测补产
[ ] 联调写入 plan_import
[ ] 验证现有成品入库流程能继续处理
```

---

## 13. 最终需求定义

智能排产模块从合同管理的 `factory_plan` 读取合同数据。系统先根据完整型号识别机型大类和版本类型，按“小机/大机/稀有机型 → XS/G/AUTO”的两层结构找到对应产线组。合同优先覆盖产线前段容量。剩余预测产能不按固定比例机械分配，而是根据老板为每个完整型号设定的目标库存比例，结合 `finished_goods_data` 中库存中机器和 `plan_import` 中生产中/待入库机器，计算未来库存比例缺口，动态选择最能修正比例的型号进行补产。老板确认开始生产后，系统生成流水号并写入 `plan_import`。这些机器未来进入 `finished_goods_data` 后，持续修正整体库存结构。
